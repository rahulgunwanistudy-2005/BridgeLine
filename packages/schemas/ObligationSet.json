{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://bridgeline.local/schemas/ObligationSet.json",
  "title": "ObligationSet",
  "description": "Deterministically derived legal implementation obligations grouped by responsible assignee and implementation context.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "assignee_kind": {
      "type": "string",
      "enum": ["teacher", "provider"],
      "description": "Kind of person responsible for the obligations in this set."
    },
    "assignee_ref": {
      "type": "string",
      "minLength": 1,
      "description": "District identifier for the teacher or provider responsible for this set."
    },
    "assignee_role": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable implementation role of the responsible assignee."
    },
    "context_kind": {
      "type": "string",
      "enum": ["student", "class", "service"],
      "description": "Kind of implementation context shared by the obligations in this set."
    },
    "context_ref": {
      "type": "string",
      "minLength": 1,
      "description": "Stable district or domain identifier for the implementation context."
    },
    "subject": {
      "type": ["string", "null"],
      "description": "Instructional subject for a class context, or null for student and service contexts."
    },
    "generated_at": {
      "type": "string",
      "format": "date-time",
      "pattern": "Z$",
      "description": "UTC RFC 3339 timestamp ending in Z when the deterministic obligation set was generated."
    },
    "rules_version": {
      "type": "string",
      "minLength": 1,
      "description": "Immutable version identifier of the deterministic rule registry used to derive these obligations."
    },
    "obligations": {
      "type": "array",
      "description": "Individual implementation requirements traceable to stable accommodations and cited deterministic rules.",
      "items": { "$ref": "#/$defs/Obligation" }
    }
  },
  "required": ["assignee_kind", "assignee_ref", "assignee_role", "context_kind", "context_ref", "subject", "generated_at", "rules_version", "obligations"],
  "$defs": {
    "Obligation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "format": "uuid",
          "description": "Stable UUID identifying this derived teacher obligation across regeneration when its source accommodation and assignment are unchanged."
        },
        "student_ref": {
          "type": "string",
          "minLength": 1,
          "description": "District identifier for the student whose approved accommodation creates this obligation."
        },
        "source_kind": {
          "type": "string",
          "enum": ["iep_record", "accommodation", "service"],
          "description": "Kind of approved IEP source from which the obligation was derived."
        },
        "source_ref": {
          "type": "string",
          "format": "uuid",
          "description": "Stable UUID of the approved IEP version, accommodation, or service identified by source_kind."
        },
        "scope_provenance": {
          "type": "array",
          "description": "Exact approved scope references that caused this obligation. Empty only for obligations sourced from an IEP record or service rather than an accommodation.",
          "items": { "$ref": "#/$defs/ObligationScopeProvenance" }
        },
        "rule_id": {
          "type": "string",
          "minLength": 1,
          "description": "Stable identifier of the deterministic rule that distributed or transformed the source accommodation into this obligation."
        },
        "citation": {
          "type": "string",
          "minLength": 1,
          "description": "Legal or policy citation supporting the deterministic rule and rendered for reviewer verification."
        },
        "action_text": {
          "type": "string",
          "minLength": 1,
          "description": "Mandatory teacher action produced exclusively by the deterministic rules engine and never authored or altered by an LLM."
        },
        "practice_text": {
          "type": ["string", "null"],
          "description": "Optional subject-specific implementation guidance authored later by the LLM, or null before brief generation."
        },
        "status": {
          "type": "string",
          "enum": ["pending", "confirmed", "flagged"],
          "description": "Teacher workflow state indicating whether the obligation awaits action, has been confirmed, or has been flagged for resolution."
        },
        "confirmed_at": {
          "type": ["string", "null"],
          "format": "date-time",
          "pattern": "Z$",
          "description": "UTC RFC 3339 confirmation timestamp ending in Z when status is confirmed, otherwise null."
        },
        "flag_reason": {
          "type": ["string", "null"],
          "description": "Teacher-provided explanation when status is flagged, otherwise null."
        }
      },
      "required": [
        "id",
        "student_ref",
        "source_kind",
        "source_ref",
        "scope_provenance",
        "rule_id",
        "citation",
        "action_text",
        "practice_text",
        "status",
        "confirmed_at",
        "flag_reason"
      ],
      "allOf": [
        {
          "if": { "properties": { "status": { "const": "confirmed" } }, "required": ["status"] },
          "then": { "properties": { "confirmed_at": { "type": "string" } } },
          "else": { "properties": { "confirmed_at": { "type": "null" } } }
        },
        {
          "if": { "properties": { "status": { "const": "flagged" } }, "required": ["status"] },
          "then": { "properties": { "flag_reason": { "type": "string", "minLength": 1 } } },
          "else": { "properties": { "flag_reason": { "type": "null" } } }
        }
      ]
    },
    "ObligationScopeProvenance": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "scope": {
          "type": "string",
          "enum": ["subject", "context", "all"],
          "description": "Applicability dimension that contributed to this obligation."
        },
        "ref": {
          "type": "string",
          "minLength": 1,
          "description": "Scope name or phrase copied from the approved IEPRecord."
        },
        "source_page": {
          "type": "integer",
          "minimum": 1,
          "description": "One-based source page supporting this scope."
        },
        "source_quote": {
          "type": "string",
          "minLength": 1,
          "description": "Verbatim source excerpt supporting this scope."
        },
        "confidence": {
          "type": "number",
          "minimum": 0.0,
          "maximum": 1.0,
          "description": "Approved extraction confidence for this scope reference."
        }
      },
      "required": ["scope", "ref", "source_page", "source_quote", "confidence"]
    }
  }
}
