# Pinned tooling for the cc/02 synthetic-dataset generators (isolated from apps/api).
# Byte-stable regeneration depends on these exact versions. Install into a dedicated venv:
#   python3.11 -m venv .venv-synth
#   .venv-synth/bin/pip install -r scripts/requirements.txt
jsonschema==4.23.0   # schema validation of ground-truth records
Pillow==11.0.0       # raster degradation + JPEG encoding for messy scans
numpy==2.1.3         # deterministic gaussian noise (fixed-seed RNG)
pymupdf==1.28.0      # build-time PDF rasterization for degradation
